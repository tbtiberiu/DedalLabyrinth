// import { UnknownAction, createSlice } from '@reduxjs/toolkit';
// import axios from 'axios';

// export const labyrinthSlice = createSlice({
//   name: 'labyrinth',
//   initialState: {},
//   reducers: {},
// });

// // export const { setGameState, setWinner } = labyrinthSlice.actions;
// const baseUrl = `https://localhost:7147/api/Game`;

// // eslint-disable-next-line @typescript-eslint/no-explicit-any
// export const betAsync = (amount: number) => async (dispatch: any) => {
//   const response = await axios.post(`${baseUrl}/bet?amount=${amount}`);
//   if (response.status === 200) {
//     dispatch();
//   }
// };

// export default labyrinthSlice.reducer;
